terraform-template-template:
  description: "Canonical meta-template for the per-type-template architecture under NWarila. Hosts universal reusable workflows synced into per-type templates (terraform-runner-template, python-template, etc.) and itself acts as the framework-shape template for terraform-framework consumers."
  visibility: "public"
  is_template: true
  topics:
    - terraform
    - template
    - project-template
    - infrastructure-as-code
    - ci-cd
    - devsecops
    - best-practices
    - hashicorp
